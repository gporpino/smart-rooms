<table class="table table-striped table-hover rooms">
  <thead>
    <tr>
      <th>Name</th>
    </tr>
  </thead>

  <tbody>
    <% rooms.each do |room| %>
      <tr data-link="<%= room_path(room) %>" class='table_row'>
        <td><%= room.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate rooms %>
