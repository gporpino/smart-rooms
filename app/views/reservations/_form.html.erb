<%= form_for [@room, @reservation] do |f| %>

  <%= render 'shared/error_messages', obj: f.object  %>

  <%= f.hidden_field :owner_id %>

  <%= f.label 'What I want to do?' %>
  <%= f.text_field :reason %>

  <%= f.label :initial_date %>
  <div class="input-append date datetimepicker">
    <%= f.text_field :initial_date, data: {format: "yyyy-MM-dd hh:mm"} %>
    <span class="add-on">
      <i data-time-icon="icon-time" data-date-icon="icon-calendar">
      </i>
    </span>
  </div>

  <%= f.label :end_date %>
  <div class="input-append date datetimepicker">
    <%= f.text_field :end_date, data: {format: "yyyy-MM-dd hh:mm"} %>
    <span class="add-on">
      <i data-time-icon="icon-time" data-date-icon="icon-calendar">
      </i>
    </span>
  </div>

  <hr>

  <%= f.submit 'Confirm', class: "btn btn-large btn-primary"%>

<% end %>
